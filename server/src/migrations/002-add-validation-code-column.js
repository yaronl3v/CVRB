#!/usr/bin/env node

/**
 * Migration to add validation_code column to worlds table
 * Usage: node src/migrations/002-add-validation-code-column.js
 */

import db from '../db.js';

async function addValidationCodeColumn() {
  try {
    console.log('Adding validation_code column to worlds table...');
    
    // Test database connection
    const connected = await db.testConnection();
    if (!connected) {
      console.error('Failed to connect to database');
      process.exit(1);
    }

    const sequelize = db.getSequelize();
    
    const addColumnSQL = `
      ALTER TABLE worlds 
      ADD COLUMN IF NOT EXISTS validation_code JSONB DEFAULT '[]';
      
      -- Add comment for the new column
      COMMENT ON COLUMN worlds.validation_code IS 'Array of validation code strings generated by validator models';
    `;
    
    await sequelize.query(addColumnSQL);
    
    console.log('✅ validation_code column added successfully!');
    
    // Show updated table structure
    const [tableInfo] = await sequelize.query(`
      SELECT column_name, data_type, is_nullable, column_default 
      FROM information_schema.columns 
      WHERE table_name = 'worlds' 
      ORDER BY ordinal_position;
    `);
    
    console.log('\n📋 Updated table structure:');
    console.table(tableInfo);
    
  } catch (error) {
    console.error('❌ Error adding column:', error.message);
    if (error.original) {
      console.error('Database error:', error.original.message);
    }
  } finally {
    await db.close();
  }
}

addValidationCodeColumn();